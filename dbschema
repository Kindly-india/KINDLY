-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.event_registrations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  event_id uuid NOT NULL,
  volunteer_id uuid NOT NULL,
  status character varying DEFAULT 'registered'::character varying CHECK (status::text = ANY (ARRAY['registered'::character varying, 'checked_in'::character varying, 'cancelled'::character varying]::text[])),
  registered_at timestamp with time zone DEFAULT now(),
  checked_in_at timestamp with time zone,
  CONSTRAINT event_registrations_pkey PRIMARY KEY (id),
  CONSTRAINT event_registrations_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.events(id),
  CONSTRAINT event_registrations_volunteer_id_fkey FOREIGN KEY (volunteer_id) REFERENCES public.volunteer_profiles(id)
);
CREATE TABLE public.events (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  organization_id uuid NOT NULL,
  title character varying NOT NULL,
  description text NOT NULL,
  cover_image_url text,
  category character varying NOT NULL CHECK (category::text = ANY (ARRAY['environment'::character varying, 'education'::character varying, 'health'::character varying, 'animals'::character varying, 'elderly'::character varying, 'community'::character varying]::text[])),
  is_urgent boolean DEFAULT false,
  event_date date NOT NULL,
  start_time time without time zone NOT NULL,
  end_time time without time zone NOT NULL,
  location text NOT NULL,
  dress_code character varying,
  things_to_bring text,
  total_slots integer NOT NULL,
  registered_count integer DEFAULT 0,
  checked_in_count integer DEFAULT 0,
  registration_deadline character varying NOT NULL CHECK (registration_deadline::text = ANY (ARRAY['1 hour before'::character varying::text, '1 day before'::character varying::text, '1 week before'::character varying::text])),
  minimum_age integer,
  status character varying DEFAULT 'published'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'cancelled'::character varying, 'completed'::character varying]::text[])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT events_pkey PRIMARY KEY (id),
  CONSTRAINT events_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organization_profiles(id)
);
CREATE TABLE public.organization_profiles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid UNIQUE,
  org_type character varying NOT NULL CHECK (org_type::text = ANY (ARRAY['registered'::character varying, 'supported'::character varying, 'informal'::character varying, 'individual'::character varying]::text[])),
  name character varying NOT NULL,
  email character varying NOT NULL,
  phone character varying NOT NULL,
  registration_type character varying,
  registration_number character varying,
  representative_name character varying,
  designation character varying,
  website character varying,
  parent_institution character varying,
  coordinator_name character varying,
  area_locality character varying,
  intent_description text,
  approval_status character varying DEFAULT 'pending'::character varying CHECK (approval_status::text = ANY (ARRAY['pending'::character varying, 'approved'::character varying, 'rejected'::character varying]::text[])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  registration_certificate_url text,
  pan_card_url text,
  proof_document_url text,
  CONSTRAINT organization_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT organization_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.volunteer_profiles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid UNIQUE,
  full_name character varying NOT NULL,
  phone character varying NOT NULL,
  city character varying NOT NULL,
  interests ARRAY,
  total_hours integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT volunteer_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT volunteer_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);